# GitHub Release Checklist

1. Create a public repository named `rensheng-mima`.
2. Push this package, not the original private workspace.
3. Confirm these paths are absent from Git:
   - `private_records/`
   - `命理记录/`
   - `.openclaw/`
   - `node_modules/`
   - `web/frontend/dist/`
   - real generated reports
4. Enable GitHub Actions.
5. Create labels: `privacy`, `engine`, `frontend`, `tests`, `documentation`, `good first issue`.
6. Open roadmap issues from `docs/roadmap-issues.md`.
7. Publish release `v0.1.0-local-mvp`.
8. Apply at `https://openai.com/form/codex-for-oss/`.

